[project]
name = "regsentinel"
version = "0.1.0"
description = "Multi-agent regulatory compliance intelligence built on the Claude Agent SDK"
requires-python = ">=3.10"
dependencies = ["claude-agent-sdk>=0.1.0"]

[project.scripts]
regsentinel = "regsentinel.orchestrator:main"

[tool.pytest.ini_options]
pythonpath = ["."]
